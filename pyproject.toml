[tool.vercel]
entrypoint = "backend/main:app"

[project]
name = "crypto-fraud-platform"
version = "1.0.0"
description = "Cryptocurrency Fraud Detection and Analytics Platform"
requires-python = ">=3.8"
